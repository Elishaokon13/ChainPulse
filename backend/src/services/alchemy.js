// src/services/alchemy.js
// Mock logic for Alchemy integration. Replace with real Alchemy SDK calls when ready.

export async function getWalletActivity(contractAddress) {
  // TODO: Use Alchemy SDK to fetch unique wallet interactions
  return 3500; // mock value
}

export async function getTransactionVolume(contractAddress) {
  // TODO: Use Alchemy SDK to fetch transaction volume
  return 45000; // mock value
} 